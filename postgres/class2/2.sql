USE coolber_rideshare;

CREATE TABLE Customers(
FIRST_NAME varchar(50),
LAST_NAME varchar(50),
PH_NO varchar(50),
EMAIL varchar(50),
FEE_NO_TIP float,
CORPORATE varchar(50),
FLAT varchar(50),
TIP float,
TYPE varchar(50),
RATING float,
MILES float,
CC_TYPE varchar(50) );
